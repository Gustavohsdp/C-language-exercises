// Você deve fazer um programa que receba três número digitados pelo usuário e os ordene em ordem decrescente.

#include <stdio.h>

int main() {

  char name[100];
  // int number1, number2, number3;
  // printf("Digite o primeiro número");
  // scanf("%d", &number1);
  // printf("Digite o se número");
  for (int i = 0; i < 2; i++) {
  printf("Informe o seu nome: ", name[i]);
  scanf("%s", &name[i]);
  printf("%s", name[i]);
  }
}